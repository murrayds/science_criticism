name: python-minimal # You can change this name to your desired environment name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10  # Specify the Python version you need
  - pandas
  - numpy
  - tqdm
  - pip
  - pip:
    - transformers
    - adapters
